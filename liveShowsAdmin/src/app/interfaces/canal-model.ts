import { Time } from '@angular/common';

export interface CanalModel {
  id: string,
  nome: string,
  idYoutube: string,
  idFacebook: string,
  idVimeo: string,
  idTwitch: string,
  status: number,
  categoria: number
}
